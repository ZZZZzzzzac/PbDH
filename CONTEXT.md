# PbDH 平台（PbDH Platform）

PbDH 平台面向 Powered by Daggerheart 规则生态，连接规则系统、游戏资源、玩家角色与卡牌桌面，但不把具体游戏规则固化进平台。

## 领域语言

本文以中文术语为主；英文名称用于代码、合约、目录及跨语言沟通。正文中的英文同义词不构成另一套领域语言。

### 角色与系统包

**系统包（System Package）**:
定义一个 Powered by Daggerheart 规则系统及其玩家角色卡能力，由玩家应用安装和运行。系统包不由资源市场分发，也不拥有全部游戏资源模板，但可以声明游戏资源进入玩家应用时的资源兼容性。
_避免使用_: 游戏资源包、主题、插件

**当前系统包（Current System Package）**:
玩家应用当前缓存、验证并运行的唯一系统包。导入另一系统包会替换当前快照，但不会删除按系统包 ID 隔离的角色存档；第一版不提供多系统包本地库。
_避免使用_: 系统包库、资源市场安装项、角色存档

**预置系统包（Preset System Package）**:
玩家应用发布物中显式登记、可通过静态加载流程取得的系统包。打开绑定其系统包 ID 的角色存档时，玩家应用可以自动切换；它不是云账号数据、资源市场商品或任意 ID 的远端下载入口。
_避免使用_: 当前系统包、用户导入系统包、资源市场系统包

**角色数据（Character Data）**:
由系统包定义字段与含义的玩家角色状态数据。平台只提供承载、校验和迁移边界，不把具体角色字段固化为全局模型。
_避免使用_: 角色存档、系统包、游戏资源

**角色存档（Character Save）**:
玩家本地优先保存的一份角色数据，绑定稳定系统包 ID，记录保存时的系统包版本与独立角色数据版本。Character Data 按有状态 Module ID 保存完整状态，例如武器栏中的最终“长剑 +2 1d8+4”字符串和各 `cardTable` 的实例状态；不保存用于恢复资源选中状态或重新解析这些字段的游戏资源引用、资源包依赖、资源库或 Entry 选中状态。玩家上传到 `imageField` 的头像或立绘通过 Asset ID 独立保存，`.pbcha` 只自包含这些玩家媒体；系统包图片和卡图字节不重复进入人物文件。每份存档可以独立同步为私有云文档。
_避免使用_: 系统包、玩家工作区、创作者工作区

**系统包作者（System Package Author）**:
设计 TRPG 规则并拥有对应系统包的低频专业角色。系统包作者通过声明式文件与 AI 可读文档定义玩家角色能力、规则入口、资源兼容性、系统表现及角色格式适配器；该身份不等同于创作者或平台管理员。
_避免使用_: 创作者、GM、模板管理员

**系统包创作流程（System Authoring Workflow）**:
供系统包作者使用的文件优先工作流，不是独立应用。目录和 ZIP 输入经过同一加载、规范化、校验与玩家运行时预览流程；普通玩家界面不暴露作者控件。
_避免使用_: 系统包作者应用、创作者应用或 GM Tabletop 功能、第二套预览渲染器

**创作者（Creator）**:
为既有或自定义规则语境创作游戏资源的角色。创作者使用资源模板或自由模板，不创建系统包，也不注册新资源模板。
_避免使用_: 系统包作者、平台管理员

**游戏主持人（Game Master，GM）**:
选择规则与游戏资源开团，并在 Creator App 中准备和操作 GM Tabletop 的角色。GM 可以使用不被当前系统包接纳的资源，也可以修改桌面实例私有的资源副本；在 Creator Workspace 中整理或创作资源时则使用完整 Creator 能力，但不因此取得来源资源包的市场发布所有权。
_避免使用_: 系统包作者、玩家、资源定义所有者

**玩家（Player）**:
在 GM 组织的游戏中使用角色、系统包、已安装资源与玩家卡牌桌面的最高频角色。
_避免使用_: GM、系统包作者、玩家应用本身

### 模板、合约与共享能力

**资源模板（Resource Template）**:
由平台管理员创建、注册并按 SemVer 版本化的可信游戏资源模具。每个精确版本拥有资源数据结构定义、校验、默认值、创作输入布局、资源表现与桌面交互行为，也是资源进入玩家应用时的兼容依据。
_避免使用_: 系统包、固定资源分类、应用内硬编码类型

**自由模板（Free Template）**:
允许创作者制作尚无专用资源模板的游戏资源的内置通用模板。自由模板仍是平台注册的可信模板，不等于用户自定义可执行模板。
_避免使用_: 自定义模板代码、未知模板、无模板资源

**模板注册表（Template Registry）**:
玩家应用、Creator App（含 GM Tabletop）与资源市场前端共同使用的可信资源模板目录，按稳定模板 ID 和精确版本解析完整模板能力。历史版本可以按需加载但不能原地覆盖；资源包只能引用注册表，不能携带模板实现代码。
_避免使用_: 资源市场模板插件、资源包内代码、按 ID 信任导入代码

**平台合约（Platform Contract）**:
跨应用、后端、文件或共享模块边界交换数据时使用的显式版本化合约，例如资源包、系统包、角色存档、卡牌桌面文档与后端 API 合约。平台合约使用 SemVer；应用版本不得充当数据兼容版本。
_避免使用_: 内部 TypeScript 类型、应用发布版本、无版本 JSON 约定

**便携归档（Portable Archive）**:
资源包或卡牌桌面文档在显式文件导入导出时使用的自包含封装，包含纯数据逻辑文档及其引用的每份规范化媒体；导出时间等传输元数据属于归档而非逻辑快照。便携归档是既有合约族的文件规格，不用于在线 API、云同步或运行时聚合。
_避免使用_: Base64 JSON、云同步文档、远程 URL 清单

**合约结构定义（Contract Schema）**:
平台合约的语言无关权威定义。文件与持久化边界使用 JSON Schema，后端 API 使用 OpenAPI；TypeScript 与 Python 类型必须由其生成或通过同一组契约样例验证。
_避免使用_: 手写类型即权威、只在前端运行的校验、文档式 JSON 示例

**开发中合约版本（Development Contract Version）**:
仅供仓库开发、测试、fixtures 与明确开发构建使用的 SemVer prerelease；它不得进入正式本地或云文档、Market、生产 API 或正式便携归档，也不承诺 Reader 或 Migration。
_避免使用_: 可发布草稿、正式用户数据版本、发布后自动追认的数据

**已发布合约版本（Published Contract Version）**:
同时具备不可变精确版本 Schema、规范正反例、稳定诊断、至少一个真实生产者与消费者的 conformance 证明，以及新 `MAJOR` 所需旧 Reader 或迁移路径的合约版本；发布后任何修正都必须使用新 SemVer。
_避免使用_: 只有 Schema 的草稿、应用内私有格式、原地修补已发布版本

**已弃用合约版本（Deprecated Contract Version）**:
停止第一方新增生产与 Market 新发布、但仍允许既有制品读取、打开、导出和显式迁移的不可变合约版本；弃用状态是平台版本元数据，不写回制品、改变其内容或改变派生摘要。
_避免使用_: 无效版本、已删除版本、自动迁移信号、既有数据撤销

**合约族（Contract Family）**:
围绕一个稳定交换边界独立演进的一组平台合约。第一版设资源包、系统包、角色存档、卡牌桌面文档和后端 API 五个合约族，彼此不共享版本号。
_避免使用_: 全平台统一版本、每个内部类型一个版本、资源模板版本

**合约迁移（Contract Migration）**:
将长期文档从不再直接支持的合约 `MAJOR` 显式转换为受支持 `MAJOR` 的确定性过程。`MINOR` 与 `PATCH` 由兼容 Reader 直接读取，不提供追版本迁移；跨 `MAJOR` 的迁移先生成并验证新副本，成功后才原子替换，不得静默扫描、猜测结构或只改版本号。
_避免使用_: 模板升级、后端 API 兼容层、加载时静默修补

**合约诊断（Contract Diagnostic）**:
由合约校验产生、包含稳定代码、严重度、合约族与精确版本、数据或文件位置及可本地化参数的结构化事实；`error` 使候选无效，`warning` 是否要求确认由拥有操作的业务 L1 决定，`info` 只作说明。
_避免使用_: 第三方校验库原始文案、warning 即统一弹窗、业务流程决定

**平台管理员（Platform Administrator）**:
唯一可以创建、注册和发布资源模板与资源转换适配器的平台治理角色。平台管理员不拥有系统包，也不等同于创作者或系统包作者。
_避免使用_: 创作者、系统包作者、资源市场作者

**桌面交互行为（Tabletop Behavior）**:
资源模板对数值轨道、状态、标记和折叠区域等运行期交互的声明。玩家应用与 Creator App 中的 GM Tabletop 只提供通用交互能力，不自行定义具体资源语义。
_避免使用_: 应用内资源类型特判、修改游戏资源定义

**桌面能力集（Tabletop Capability Set）**:
宿主授予共享桌面核心与 React 交互层的通用命令集合。玩家应用和 Creator App 中的 GM Tabletop 分别决定开放哪些桌面能力；未授权命令不得仅因界面入口存在而执行。该能力集不限制 Creator Workspace 的资源创作能力。
_避免使用_: 角色权限系统、GM 专用桌面核心、仅隐藏按钮

**规范卡面（Canonical Card Surface）**:
资源模板完成并声明权威卡牌表面。固定比例卡使用 `63:88` 设计坐标；可变高度卡与固定比例卡等宽，只允许高度变化。它由当前结构化资源定义、精确模板版本、媒体资产和可选运行状态实时生成；这些设计坐标不是屏幕或打印的实际毫米尺寸，宿主不能据容器重新排版内部内容，GM 修改桌面实例资源副本后仍由同一资源渲染器生成。
_避免使用_: 把 63 当成实际显示毫米、90×142 卡面、宿主重新排版、应用专用卡面、预渲染权威图片

**资源渲染器（Resource Renderer）**:
所有前端共同使用的规范卡面渲染入口。宿主提供游戏资源或桌面实例资源副本、媒体资产、可选运行状态和外部显示尺寸；共享入口把固定卡面锁为 `63:88`，把可变高度卡锁为相同宽度和声明高度。宿主不能替换卡面内部实现或样式。
_避免使用_: 每个应用一套渲染器、兼容新版静默重画旧版本

**渲染器修订版（Renderer Revision）**:
模板注册表内部的一份不可变卡面渲染实现。每个精确资源模板版本固定解析到一个修订版；渲染行为相同的多个模板版本可以共享同一修订版。
_避免使用_: 每张资源一份渲染器、模板版本的同义词、可原地修改的组件

**托管媒体资产（Managed Media Asset）**:
图片按确定的裁剪、缩放与 WebP 编码规范化后，由平台后端按内容哈希保存并通过稳定资产 ID 引用。资产默认私有；多个文档、资源和市场出版物可以引用同一物理媒体，账号配额按该账号拥有的唯一媒体字节计算。
_避免使用_: 工作区 Base64 图片、每个资源复制一份图片、公开哈希 URL、Supabase Storage 对象

### 资源转换

**资源转换核心（Resource Conversion Core）**:
玩家应用、卡牌工坊与非交互脚本共用的无界面、无持久化转换模块。调用方明确选择来源格式后，核心通过可信适配器输出逐记录报告和游戏资源候选，不让第三方数据结构进入持久化边界。
_避免使用_: 跨生态自动检测、工作区导入、资源管理器持久化、角色转换

**来源格式（Source Format）**:
用户在上传入口明确选择的外部资源生态，例如 dhsheet、ZZZ、RinkCX、基德或 PbDH。来源格式不从文件内容猜测；选择错误时直接报告不匹配。
_避免使用_: 文件扩展名、容器类型、目标系统包

**资源转换适配器（Resource Conversion Adapter）**:
由平台管理员创建、注册并按 SemVer 版本化的可信转换代码，负责一种来源格式的容器解析、来源数据校验、模板映射、媒体候选和损失报告。它只能输出符合已注册资源模板模式的游戏资源候选，不能直接持久化。
_避免使用_: 系统包脚本、用户映射、万能中间格式、数据库导入器

**游戏资源候选（Game Resource Candidate）**:
资源转换适配器产生的短生命周期规范候选，已用 PbDH 模板 ID、模板版本、结构化数据和媒体候选表达，但尚无最终平台身份、所有权或安装状态。用户确认后，调用方才为其分配身份并持久化。
_避免使用_: 已安装游戏资源、第三方来源数据、创作者草稿

**角色格式适配器（Character Format Adapter）**:
由系统包作者随系统包提供、受合约约束的转换器，负责一种外部人物格式与该系统角色数据之间的导入或导出。玩家应用负责明确来源、隔离执行、诊断和最终校验；适配器不能直接写入角色存档，也不进入资源转换适配器注册表。
_避免使用_: 资源转换适配器、角色存档版本迁移、平台全局人物格式插件

**角色存档候选（Character Save Candidate）**:
角色格式适配器导入后产生的短生命周期候选，已使用角色存档合约外层结构和目标系统包的角色数据表达，但尚未持久化。玩家应用完成两层校验并取得用户确认后，才创建新角色存档。
_避免使用_: 第三方人物数据、已持久化角色存档、游戏资源候选

### 游戏资源与资源包

**资源兼容性（Resource Compatibility）**:
系统包对可进入其玩家原生资源入口的资源模板作出的显式声明。玩家应用按每个资源的精确模板 ID 与版本检查当前系统包声明；命中后映射到声明的原生资源入口，不要求资源包重复指定目标系统。未命中声明或本地无法执行精确模板的有效资源仍可安装，但进入其他资源。
_避免使用_: 资源包目标门禁、安装门禁、跨系统包访问限制、按字段猜测、资源作者单方面声明、仅字符串相等

**目标系统包引用（Target System Package Reference）**:
资源包对一个目标规则系统的可选分发与发现声明，由稳定系统包 ID 与精确 SemVer 版本组成；一个资源包可以声明零个或多个引用。引用不参与 Player 原生路由、安装或访问控制；Player 只依据当前系统包的资源兼容性与资源模板身份路由。Creator App（含 GM Tabletop）和资源市场只保存、展示并校验引用结构，不校验所引用系统包是否存在或兼容。
_避免使用_: 原生路由门禁、安装权限、Creator App 或 GM Tabletop 规则模型、应用版本、单系统访问限制

**游戏资源（Game Resource）**:
供玩家或 GM 引用和操作的内容，例如 NPC、敌人、环境、职业、子职、领域卡、武器、护甲和物品。它在所属资源包内使用稳定且唯一的资源 ID，可以标注精确资源模板版本和普通标签；Creator App 中的“规则”只是一种标签。
_避免使用_: 系统包、Creator App 或 GM Tabletop 规则对象、角色数据、任意附件

**游戏资源引用（Game Resource Reference）**:
由全局唯一资源包 ID 与包内资源 ID 组成的稳定复合引用。来源追踪和其他需要跨包定位资源的数据不得只保存资源 ID；模板版本与资源内容版本不属于身份键。角色存档中的人物卡字段保存资源应用后的最终值，不保存游戏资源引用。
_避免使用_: 裸资源 ID、资源标题、桌面资源快照 ID

**资源应用（Resource Application）**:
玩家在玩家应用的资源库 Module 中主动选择游戏资源后发生的一次性写入交互。系统包以声明式 Dependency 定义来源 Module、选择事件、目标 Module、字段读取、格式和写入动作；玩家应用框架解释并原子执行声明，把最终文本或数值写入 Character Data。完成后不保存资源包、资源库、Entry、游戏资源引用、来源或选中状态；例如武器 `freeText` 与姓名 `freeText` 对玩家应用没有语义差别。
_避免使用_: System Package 主动写入、持续资源绑定、资源字段转换、加载时重新解析

**其他资源（Other Resources）**:
玩家应用保存的非原生路由资源集合：资源未命中当前系统包的资源兼容性，或当前应用无法执行其精确模板。资源管理器始终允许查看和管理；系统包可以声明其他资源库 Module 与 Dependency，使玩家主动选择这些资源并把结果写入角色数据或卡牌桌面。
_避免使用_: 安装失败、丢弃资源、自动猜测兼容类型、不可使用资源

**资源管理器（Resource Manager）**:
玩家应用中安装、浏览、更新和移除资源包及游戏资源的入口。每个有效资源包快照只安装一份；它依据当前系统包的资源兼容性与每个资源的模板身份生成原生入口或其他资源视图，但不修改资源定义。切换当前系统包会重新计算视图，不复制资源包。
_避免使用_: 资源市场、创作者工作区、系统包管理器

**资源包（Resource Package）**:
包含一个或多个游戏资源、具有全局唯一稳定 ID 和明确版本、并作为整体安装的分发单元；一个资源包可以声明零个或多个目标系统包引用，目标集合只表达可选分发与发现元数据，不限制安装、原生路由或其他资源使用。正式目录与 ZIP 便携归档严格同构：非空目录从资源路径推导，根入口的 `emptyDirectories` 只记录没有后代文件的目录；资源列表按文件名、时间等明确键确定性排序，不保存手动顺序。在线流转仍使用纯数据与独立媒体接口。
_避免使用_: 单卡、系统包、卡牌桌面文档

**资源包快照（Resource Package Snapshot）**:
资源包在某个明确版本下形成的不可变副本，用于 Creator App、资源市场和玩家本地资源库之间传递。GM Tabletop 不维护另一份资源库；它从 Creator Workspaces 显式放置当前资源。上游修改不会改变既有快照；同一资源包 ID 的新快照只有在用户显式导入或更新后才替换本地旧快照。
_避免使用_: 创作者草稿、桌面资源快照、实时远端引用

**资源包版本（Resource Package Version）**:
资源包显式快照的 SemVer；平台只按稳定引用与结构变化计算最低建议——删除/改 ID、不兼容 Template `MAJOR`、删除目标系统引用、改变目标系统 ID/`MAJOR` 或关系断裂为 `MAJOR`，新增资源、非破坏关系或新增目标系统引用为 `MINOR`，字段、媒体、标签、路径、展示元数据、许可、来源及既有目标系统 `MINOR`/`PATCH` 变化为 `PATCH`；目标集合顺序不构成内容变化——作者可选择更高版本但不得低于可证明最低值。
_避免使用_: 游戏规则兼容结论、自动平衡判断、Snapshot Digest、应用版本

**资源包快照摘要（Resource Package Snapshot Digest）**:
由系统按精确 Contract 规定的 SHA-256、RFC 8785 JCS、确定顺序、带类型/长度边界的字节流和用途域标识，从包逻辑内容及规范化媒体派生的摘要；普通文本保留原始 Unicode 码点，路径另按可移植路径规则规范化，便携归档声明值不参与自身计算且消费者只信任重算结果，它仅用于精确相等、完整性、幂等及同版本异内容检测，不是身份、版本、来源、所有权、作者字段、签名或真实性证明。
_避免使用_: Snapshot 身份、内容版本、作者填写的 Hash、许可或可信证明

**资源许可（Resource License）**:
随资源包快照保存并在市场出版物中展示的标签与人类可读声明字符串。PbDH 只保存和展示，不解释、验证或执行其法律含义，也不据此允许或禁止编辑、Fork、导出、发布、安装或发送到桌面；用户自行负责遵守声明和适用法律。
_避免使用_: 平台权限、功能门禁、兼容认证、自动法律判断

**派生来源（Fork Provenance）**:
每个可发布资源包快照都必须携带的可空包级字段；空值表示没有上游。只有用户从 Market 中一个已经发布的资源显式发起改编、并最终发布新 Package ID 时，非空值才形成 Fork 关系，记录来源 Publication、资源包 ID、版本或快照摘要及来源游戏资源引用。普通导入、复制、冲突另存和未发布草稿不会自动成为 Fork。
_避免使用_: 所有权转让、跨包运行依赖、裸文本致谢

### 卡牌桌面

**桌面实例资源副本（Tabletop Instance Resource Copy）**:
游戏资源每次放入玩家应用或 GM Tabletop 时，由一个桌面卡牌实例私有拥有的独立资源定义副本。它记录可空的来源游戏资源引用、精确模板版本、结构化数据、标签和资产 ID；由多个输入合成且不存在唯一来源资源的实例使用空来源。GM 对副本的修改只影响所属实例，来源资源、其他实例和媒体字节不变。
_避免使用_: 桌面资源快照、共享资源定义、当前资源库引用、游戏资源主记录

**桌面形态替换（Tabletop Replacement）**:
由当前桌面实例携带的显式形态关系，用于在执行时定位同一 Creator Workspace 中的目标游戏资源，并以目标当前内容建立新的桌面实例。它不是桌面文档中预存的隐藏形态、Replacement 闭包、形态历史或与工作区的持续绑定。
_避免使用_: 缓存形态、预物化形态、桌面内资源闭包、自动刷新

**卡牌桌面文档（Tabletop Document）**:
玩家应用与 GM Tabletop 共用的版本化桌面数据模型，拥有当前桌面的卡牌实例、每实例独立资源副本、布局与运行状态。Player 的 Tabletop Document 数据嵌在所属 Character Data 中，随 Character Save 保存、同步和导出，没有独立桌面 ID 或 `.pbtab`；GM Tabletop 则用独立文档信封承载同一模型，一份文档只表示一张桌面，并可单独同步及导入导出 `.pbtab`。
_避免使用_: 只有 GM 才使用的桌面模型、Player 独立桌面生命周期、资源包、Creator Workspace、内嵌图片的云 JSON

**桌面卡牌实例（Tabletop Instance）**:
游戏资源在某个卡牌桌面上的一次独立使用，拥有自己的桌面实例资源副本、位置、层级和模板声明的运行状态。桌面形态替换会以目标资源建立新实例来取代当前实例，只延续位置、尺寸、旋转与层级；当前实例不直接解析资源库，也不把资源副本或运行状态写回来源游戏资源。
_避免使用_: 游戏资源、资源草稿、共享资源定义、当前资源库引用

### 产品域、账号与云

**创作者工作区（Creator Workspace）**:
创作者应用中持续创作游戏资源的私有、本地优先工作区。编辑先写入本地存储，再异步云同步；网络或配额异常不得阻止本地编辑和文件导出。
_避免使用_: 资源包、资源市场发布页、GM 卡片桌面

**卡牌工坊（Creator App）**:
用于创建和编辑游戏资源、管理 Creator Workspaces、组装资源包、文件导入导出及发起资源市场发布的应用与 L1 产品。它也是 GM Tabletop L1 的唯一前端入口和部署宿主；共享应用不合并 Creator Workspace 与 Tabletop Document，也不创建系统包或资源模板。
_避免使用_: Creator Workshop、独立 GM App、统一可变大工作区、系统包创作流程

**GM 卡片桌面（GM Tabletop）**:
供 GM 准备、展示和操作资源卡片的独立 L1 产品能力，在 Creator App 中表现为特殊 Tabletop Document 标签页，不是独立 App、入口或部署物。它可以浏览全部 Creator Workspaces，但只有 GM 的显式放置动作才把当前资源复制为桌面实例；之后两侧不自动更新、合并或回写。
_避免使用_: GM App、GM Resource Workspace、玩家卡片桌面、统一 Creator/Tabletop 文档、游戏资源库

**云同步（Cloud Sync）**:
玩家应用与 Creator App 共用的后台同步能力。角色存档、Creator Workspace 与 GM Tabletop Document 使用同一云文档服务但保持独立文档种类、所有权和 payload；登录前已有本地文档必须由用户显式复制到云端，登录后新建或明确导入的文档先写本地再自动同步。账号会话被新端接管后，旧端停止云写入但保留本地编辑与导出能力。

**平台回收站（Platform Recycle Bin）**:
角色存档、Creator Workspace 与 GM Tabletop Document 共用的恢复入口。未同步文档删除后仍留在本机，已同步文档删除后进入云端；列表必须标明文档种类与“本机 / 云端”，两者都保留 30 天并支持恢复和永久删除。平台回收站只统一界面与生命周期，不合并三类文档的数据所有权，也不会因登录把本机内容上传云端。
_避免使用_: 在线编辑前提、实时协作、跨文档事务、资源市场存储

**PbDH 账号（PbDH Account）**:
玩家应用、Creator App 与资源市场共同识别的统一用户身份，使用稳定账号 ID。统一身份不合并不同领域文档拥有的数据或权限；注销账号会清理身份资料和私有数据，但不级联删除市场出版物。
_避免使用_: 创作者专用账号、资源市场独立账号、所有用户数据的聚合根

**平台网页外壳（Platform Web Shell）**:
在同一浏览器 origin 中承载玩家应用、Creator App、GM Tabletop 与资源市场的唯一用户前端入口，拥有全局账号会话、Platform App Bar 和主页面路由。它只组合各产品 Surface，不拥有或合并角色存档、Creator Workspace、Tabletop Document、已安装资源包或 Market Publication 状态。
_避免使用_: 第五个产品域、统一业务工作区、跨端口会话桥

**平台通知（Platform Notification）**:
由 Platform App Bar 右上角通知入口统一保存和展示的应用级短消息，例如发布成功、导出完成、同步失败或后台读取失败。Player、Creator、GM 与 Market 不渲染自己的限时气泡、成功横幅或另一套通知队列。要求用户当场处理的表单错误、确认窗口、导入诊断和当前组件加载状态仍留在产生它们的界面内，不进入平台通知。
_避免使用_: App 私有 toast、底部限时气泡、把表单校验或阻断诊断塞进通知列表

**平台后端（Platform Backend）**:
承载身份、云同步、托管媒体资产与资源市场能力的模块化单体。各模块分别拥有数据仓储和授权规则；Supabase 只提供外部身份认证，不承载 PbDH 结构化数据、云文档或媒体流量。
_避免使用_: 无领域边界的单体、首版微服务集群、任一前端应用的本地运行依赖

**玩家应用（Player App）**:
玩家使用角色、当前系统包、已安装资源和玩家卡牌桌面的独立应用。它以成熟的 PbDH Sheet 玩家行为为迁移基线；所有结构有效的资源包都可安装，匹配当前系统目标与资源兼容性的资源进入系统包原生入口，其余资源进入其他资源。
_避免使用_: Sheet 仓库、基础框架（Base Framework）

**资源市场（Market）**:
联网使用的公共游戏资源发现与分发入口，提供包级与单卡级搜索、浏览、稳定分享链接及完整资源包取得。它不发布系统包，也不是任何应用本地启动、文件导入导出或已安装资源使用的前提。
_避免使用_: 系统包市场、Creator App 内部模块、应用启动依赖

**市场出版物（Market Publication）**:
资源市场为一个资源包 ID 建立的服务器权威公共记录，绑定单一作者、稳定链接、独立展示信息和唯一当前资源包快照；它不是云文档，也不保存公开历史版本库。
_避免使用_: 创作者工作区、账号私有文档、跟随作者草稿变化的视图

市场出版物只有在取消发布后才能由作者或平台管理员永久删除，操作使用一次明确的确认窗口。删除会使市场记录、稳定链接和市场保存的当前资源包快照失效，但不删除 Creator 原稿，也不能召回已经取得的副本；资源包 ID 的既有所有权仍保留，原作者以后重新发布时会创建新的市场出版物与稳定链接。

## 领域关系与约束

### 角色与产品边界

- 内容生产与使用层级是：系统包作者 → 创作者 → GM → 玩家。系统包作者定义规则，创作者补充游戏资源，GM 使用规则与资源组织游戏，玩家参与游戏。
- 使用频率依次为：玩家 > GM > 创作者 > 系统包作者 > 平台管理员。产品默认路径首先优化高频角色；越低频的操作越应使用直接、可审查、文件优先或仓库优先的流程，不为低频治理预先建设复杂应用、动态扩展或多层交互机制。
- 平台管理员是最低频的平台治理角色，不属于游戏内容层级，也不因平台权限拥有任何系统包。
- 资源转换适配器由平台管理员治理，因为它输出平台管理员定义的资源模板；角色格式适配器由系统包作者提供，因为它输出系统包作者定义的角色数据。
- PbDH 账号可以关联同一个人承担的多个角色，但共享身份不合并各角色拥有的数据或权限。
- 第一版系统包作者只使用系统包创作流程；不建设独立系统包作者应用，也不把系统包编辑能力放进 Creator App 或 GM Tabletop。
- 系统包属于玩家应用领域；资源市场、Creator App 和 GM Tabletop 不远程读取或运行系统包，只通过平台合约与资源模板互操作。
- Creator App 与 GM Tabletop 不建立规则系统实体；“规则”只是游戏资源标签，GM Tabletop 不按标签、系统包或资源兼容性限制可放入的资源。
- Creator App 与 GM Tabletop 是不同的 L1 产品边界，但共用 `apps/creator` 入口和部署。Creator App 拥有 Creator Workspaces；GM Tabletop 拥有 Tabletop Documents。共享应用不形成统一可变文档。
- GM 在左侧资源区进行整理、复制、导入、转换或编辑时使用完整 Creator 能力。GM Tabletop Capability Set 只约束桌面命令与实例私有定义编辑，不限制 Creator Workspace。
- Creator 与 GM 共用左侧 Creator Workspace 的危险操作流程；工作区删除、清空等破坏性操作使用 Creator 拥有的警告与确认。GM Tabletop 上的实例修改、移除、清空、放置和状态变化直接执行，第一版不提供撤销、重做或持久命令历史。

### 身份、兼容性、快照与副本

- 资源市场可以按规则标签帮助发现资源，但标签不构成资源兼容性；当前系统包的显式声明才是玩家应用兼容判断的依据。
- 第一版每个资源包可以声明零个或多个目标系统包引用，作为可选的分发与发现元数据。所有结构有效的资源包都可以安装；玩家应用逐资源按当前系统包声明的模板兼容性尝试原生路由，未命中或无法执行精确模板的资源进入其他资源。Creator App、GM Tabletop 与 Market 不执行 Player 原生路由。
- 系统包 ID 与资源包 ID 全局唯一且跨版本稳定；资源 ID 只在所属资源包内唯一。资源模板 ID 与资源转换适配器 ID 由平台管理员分配。
- 所有权独立于 ID，不能编码在用户名等可变账号资料中。资源市场首次接纳资源包 ID 时绑定发布者账号；从 Market 显式发起并最终发布的 Fork 必须取得新资源包 ID。
- 第一版资源包不依赖其他资源包；桌面形态替换与其他资源关系必须使用包内游戏资源引用。每个可发布快照保存资源许可与必填可空 Fork 来源；普通草稿操作不自动生成上游。
- 资源包在 Creator App、资源市场与玩家边界之间按快照传递。Creator App 的普通导入、更新、no-op、取消、显式替换与另存新草稿规则是 GM 准备资源时同样使用的唯一可变包规则；不存在单独的 GM 同 ID 替换模型，也不自动 Fork。
- 用户在资源库 Module 中选择游戏资源时，玩家应用框架执行当前系统包声明的 Dependency，并把最终结果原子写入 Character Data；系统包声明映射但不主动执行。角色存档不保存用于恢复选中状态或重新解析字段的游戏资源引用、资源包依赖、资源库或 Entry ID。后续资源更新、移除或 Market 状态变化都不能改变已经写入的人物卡结果。
- 游戏资源每次放入卡牌桌面时都形成该实例独立拥有的资源副本，不与同来源的其他实例共享。资源库更新不改变既有桌面卡；同一游戏资源引用的新旧定义和 GM 修改版可以在一张桌面中并存。
- GM 可以修改桌面实例资源副本中符合其精确资源模板版本的数据；修改只影响当前实例，不改变模板、渲染器、来源游戏资源或其他实例。玩家应用是否开放同类修改由其桌面能力集决定。
- 工作区资源与桌面实例之间不存在刷新、重绑定或反向保存动作。使用工作区新版资源必须再次显式放置，或执行当前实例声明的桌面形态替换；复制桌面实例只复制该实例当前资源副本与运行状态。
- 每次桌面形态替换都重新查找执行时同一 Creator Workspace 中的目标资源并建立新实例，不复用曾经成功切换过的内容。成功时只沿用位置、尺寸、旋转与层级，目标资源的运行状态重新初始化；目标缺失或无效时当前实例和桌面文档保持不变。
- Tabletop Document 与 `.pbtab` 只保存当前桌面实例，不保存隐藏形态、历史形态或 Replacement 闭包；切回先前形态仍必须在执行时找到对应工作区资源。

### 模板、渲染与媒体

- 固定比例规范卡面只使用 `63:88` 设计比例；可变高度卡与固定卡等宽，只允许高度变化。这里的数值是卡面设计坐标，不是屏幕或打印的实际毫米尺寸。
- 宿主另行决定实际显示大小。需要固定比例的桌面、列表或详情框使用 `63:88` 外框等比缩放并截断超出部分；宿主不能改变卡面内部内容、换行和布局。
- 已发布资源模板的精确版本不可变并完整保留。旧资源可以继续查看、操作和编辑；升级模板必须由创作者显式选择。
- 同一当前结构化资源定义、精确模板版本、媒体资产与模板声明的运行状态必须产生同一规范卡面。Creator App 的资源视图、资源市场和资源目录使用游戏资源与默认状态；玩家应用或 GM Tabletop 使用实例资源副本与实例状态。
- 图片规范化后成为托管媒体资产。多个资源和文档复用同一资产 ID；逻辑文档和云同步不得重复嵌入图片字节。
- 托管媒体资产默认私有；资源市场发布为同一物理媒体增加公共引用。取消发布无法撤回客户端或缓存已经取得的字节。
- 公开资源被有效取得并写入 Character Save、Creator Workspace 或 GM Tabletop Document 时，文档为所需资产建立持久、非所有权、非配额的私有读取引用。Publication 后续取消发布、平台下架、版权处理或作者注销只停止新的公共取得，不能让既有文档缺图，也不能阻止其在新设备恢复；文档永久删除或回收站到期才释放该引用。
- 资源包与卡牌桌面文档在线流转时只保存资产 ID；只有显式文件导入导出的便携归档自包含所需媒体，每个资产 ID 携带一次。
- 资源包导入或资源市场安装必须在合约、游戏资源和必需媒体全部验证后提交。本地内容寻址资产库按资产 ID 去重，完成安装后不依赖资源市场或远程图片 URL。
- PbDH 账号配额只统计该账号拥有的托管媒体资产字节；纯 JSON 云文档不占额度，安装他人的市场资源不产生新的云端副本或额度。
- 注销 PbDH 账号不删除市场出版物。出版物转为“已注销作者”的只读状态，仍被引用的媒体由平台托管；取消发布、平台下架和版权删除使用独立生命周期。
- Market Publication 对一个资源包 ID 只有一个当前公开快照。系统派生的 Snapshot Digest 只用于判断精确相等：相同版本与相同摘要的重复发布幂等，相同版本但摘要不同或非递增版本被拒绝；摘要不参与身份或新旧排序。稳定 Publication 链接始终指向当前快照，单卡深链接使用 Publication ID 与包内 Resource ID。
- Market 可以索引包与单卡，但下载、安装到 Player、发送到桌面或在 Creator 中改编都传递当前完整资源包。“发送到桌面…”会先通过 Creator Resource Package Ingress 完成导入、更新或显式冲突处理，再让用户选择一张已有 GM Tabletop Document；没有桌面时提示新建。该路径不创建 Fork，选择或新建也只打开目标桌面并保留来源资源焦点，绝不自动放置。只有 Market 上单独、显式的改编动作可以创建带待发布上游来源的新草稿。
- 匿名用户可以搜索、浏览、分享、下载和发起 App 交接；发布与出版物管理要求已验证账号。Market Publication 是在线、服务器权威记录，不进入 Cloud Document Service 或离线 outbox。
- 作者取消发布、平台审核下架与版权删除是不同状态，均不能召回已下载快照。作者可在重新校验后重新发布原 Publication；审核或版权状态只能通过对应治理流程解除。

### 合约、云同步与玩家数据

- 共享代码依赖保持单向：`contracts` 位于底层，模板与卡牌桌面分离无 React 的核心入口和前端入口；平台后端不得依赖 React 或浏览器代码。
- 资源包、系统包、角色存档与卡牌桌面文档是长期用户数据。停止读取旧合约主版本前，必须保留旧读取器或提供显式合约迁移；后端 API 可以按通信协议独立退役。
- 第一版玩家应用只保留一个当前系统包；角色存档按稳定系统包 ID 隔离，切换当前系统包不会删除其他系统包的角色数据。
- 登录前已存在的角色存档、Creator Workspace 与 GM Tabletop Document 都不得因登录而静默上传；用户显式复制到云端后，本地内容成为同一文档 ID 的离线缓存。登录后新建或明确导入的有效文档默认进入后台同步。
- 打开云端角色存档时，玩家应用按系统包 ID 解析依赖：预置系统包可自动切换，未知系统包要求用户手动导入。角色存档不携带系统包本体。
- 角色存档与资源包保持分离。已经写入 Character Data 的资源结果不依赖原资源包存在，打开或恢复角色时不重新解析、刷新或补齐这些字段；资源管理器只为之后的新选择提供候选。玩家桌面实例依靠其自包含副本恢复，而不是依赖当前已安装资源包。
- PbDH Sheet 的成熟玩家行为默认保留；游戏资源、资源安装和资源管理器是为对齐平台合约与资源模板而重构的边界。
