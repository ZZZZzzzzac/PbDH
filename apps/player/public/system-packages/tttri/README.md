# TTTRI System Package

## 内容更新来源

后续规则与资源更新请查阅[飞书知识库](https://lcnz1nhz1wq6.feishu.cn/wiki/ZOFBw9A4NirdRHkri0Wc1PlVnEF)。

## 护甲迁移补漏（2026-09-08）

内置 `resources/tttri.pbres` 2.1.2 恢复 34 件 TTTRI 护甲，总计 607 条资源。来源为只读 `PbDH_Sheet` 仓库 commit `16396c6c36a814c4cf067d159b6bbef1310a1d7a` 的 `public/system-packages/tttri/resources/armor.json`。

保留原 ID、名称、位阶、阈值、护甲值及规则全文；`描述` 按首个中文冒号拆成护甲模板 1.0.1 的 `特性名称` 与 `特性描述`，空描述保留空特性。路径为 `护甲/位阶N/名称.json`，来源标签沿用“罗德岛旅记官方资源”，没有新增图片。原有 573 条资源及媒体不变。通过正式 PBRES 封包、摘要计算与回读校验，内置索引同步新摘要，使现有安装自动更新。

## 消耗品与物品栏选择（2026-09-20）

内置 `resources/tttri.pbres` 2.1.3 新增 60 件 TTTRI 消耗品，总计 667 条资源。来源为飞书文档「TTTRI第一批消耗品快照」（`XQc8dMguno9xaDxy0tPcKaqenqf`）内嵌表格 `Eqy4svf1Chu8pLtSOM6cTfP2ntb` / `Xs1Ffr`。

每条消耗品使用物品模板 1.0.1：`名称` 取表格「战利品」列，`掷骰` 取「掷 骰」列的两位数编号（01-60，对应文档掷骰指南 d12 之和），`特性描述` 取「效 果」列并统一为半角括号，`类型` 为「消耗品」，`原文` 与 `简介` 留空。路径为 `物品/消耗品/名称.json`（名称中的 `/` 写作 `-`），来源标签沿用“罗德岛旅记官方资源”，没有新增图片。原有 607 条资源及媒体不变。

人物卡物品栏标题行新增 `pick-inventory-item` 选择按钮（资源库 `loot`，多选，默认按 `掷骰` 升序），选中后按 `- **名称**: 特性描述` 追加进 `inventory`；`layouts/character-main.html` 与 `skins/rhodes-island`、`skins/terra-portal` 两个皮肤同时把该模块挂在 `物品栏 + INVENTORY` 的 `h2.section-title` 同一行（`inventory-heading-row`），并由 `layouts/base.css` 把按钮缩小到 1.65rem × 1.35rem 以适配标题高度。系统包 `resourceCompatibility` 中原有的 `物品 → loot` 映射为该按钮提供资源库。

## 子职批次更新（2026-09-20）

内置 `resources/tttri.pbres` 2.1.5 新增 14 个子职，共 70 条资源，总计 737 条资源。来源为飞书文档「2026/09/20职业分支新增内容快照」（`UaBSdJigUoC5syxD39kcxAgnnvb`），通过 `lark-cli drive +export --file-extension docx` 导出 `.docx` 后解析 `word/document.xml`，以保留文字染色信息（`docs +fetch` 的 XML/Markdown 通道会丢失颜色）。

新增子职：先锋〖爆破手〗〖速援手〗、近卫〖教官〗〖剑豪〗、重装〖巡卫〗〖本源铁卫〗、狙击〖炮手〗〖裂空炮手〗、术师〖荒野术师〗〖本源术师〗、辅助〖守望者〗〖巫役〗、特种〖炼金师〗〖陷阱师〗。每个子职展开为 5 条子职业模板 1.0.1 资源（`T1/T2/T3/T4X/T4Y`，`等级` 对应预备/正式/资深/精英X/精英Y），路径为 `干员/主职/子职/阶段.json`，来源标签沿用“罗德岛旅记官方资源”，没有新增图片。7 条 `职业` 资源与既有 56 个子职资源不变。

字段映射：`推荐次领域` 取「推荐搭配领域」并去掉【】；`武器原型` 由「武器原型」+「武器伤害骰」合成（精英干员段只给调整值，取资深段骰值 +3，即基础 +9）；`特性` 按阶段取对应小节（T1 子职特性、T2 子职特性提升、T3 职业特性提升与子职特性二次提升、T4X 希望特性提升、T4Y 追加第二职业特性）；`子职提升` 为对应小节全文加标签前缀（子职特性获得/增强、职业特性增强、希望特性增强、职业特性追加）；`子职特性`、`职业特性`、`希望特性` 为去掉颜色标记后的纯文本，T4Y 的 `职业特性` 为资深段内容加追加特性。

docx 文字染色按受限 Markdown 落盘：红 `d83931`→`:red`、橙 `de7802`→`:orange`、绿 `2ea121`→`:green`、紫 `6425d0`→`:purple`、灰 `8f959e`→`:gray`，正文染色统一包成 `:color[**文本**]`，灰色斜体注释段落为 `*:gray[**文本**]*`；小节标题本身的颜色只用于指示改动范围，不写入资源。

已知源文档笔误（按权威文档原样保留）：子职〖速援手〗资深干员段的职业特性名称写作「一鼓作气-破术者」。

## 子职模板分治（2026-09-20）

内置 `resources/tttri.pbres` 2.1.6 把 350 条子职资源从 daggerheart 的 `子职业@1.0.1` 重新标记到专用模板 `罗德岛子职@1.0.0`，资源总数仍为 737 条（子职 350 条、消耗品 60 条）。本批次只重标 `template` 字段，资源的 `data` 内容一字未改，路径、媒体与来源标签不变。

罗德岛子职模板承载 TTTRI 独有字段（`阶段`、`推荐次领域`、`武器原型`、`子职提升`、`子职特性`、`职业特性`、`希望特性`），与 daggerheart 的子职业模板分离：`子职业` 回归 daggerheart 专用，只保留 `名称`/`原文`/`类型`/`主职`/`等级`/`施法属性`/`特性`/`简介` 8 项；本包子职的 `职业特性` 因此取资源自身字段，不再回落到所属职业。

系统包 `resourceCompatibility` 新增 `罗德岛子职 → subclasses(干员)` 映射，既有 `子职业 → subclasses(干员)` 映射保留。重标后包内摘要为 `sha256:67fe3f4dfe7c33ba2c148f2e382c539712036a5b7b60476ed18ad90399b5a894`，`apps/player/src/tttri-preset.generated.json` 的 `embeddedResourceIndex` 已同步该摘要，既有安装会据此自动重装资源包。

## 元素损伤说明卡与其他资源入口（2026-09-20）

内置 `resources/tttri.pbres` 把子职里重复的「元素损伤」注释清单抽成独立卡：新增 1 条 `说明:元素损伤`（自由模板 1.0.1，路径 `说明/元素损伤.json`，类型「说明」），不写简介，唯一内容条目的名称与原文留空，描述以 `- ` 前缀列出神经损伤、侵蚀损伤、凋亡损伤、狂躁损伤、灼燃损伤与元素伤害的通用规则（已封包数据经真实自由模板渲染器输出为单个 `<ul>` 与 6 个 `<li>`，无 `<br>`）。12 条资源共 180 处重复注释段被移除（术师〖本源术师〗、辅助〖巫役〗、重装〖本源铁卫〗的 T1–T3，以及术师〖荒野术师〗、狙击〖炮手〗、狙击〖裂空炮手〗的 T4Y），被抽走清单的 36 个字段改为在末尾留下灰色斜体指引「详情见其他资源里的元素损伤卡」。资源总数 737 → 738，说明卡摘要随包版本推进至 `sha256:549fd49336110add729c2b5ad22112fd334244dbc864ddabb9db23ed4653dfed`（包 2.1.9）。

同批次按当前飞书文档（wiki `ZOFBw9A4NirdRHkri0Wc1PlVnEF`，经 docx blocks 通道读取以保留 `text_color`）重刷 56 支子职的 280 条资源，修正 4 处字段缺陷：先锋〖策士〗T1 武器原型 双刃剑 → 刀剑、特种〖怪杰〗T4X/T4Y 武器原型 钩索装置 → 混合弹药型铳械（远距离/双手 d10+9/物理）、重装〖哨戒铁卫〗T2 补回子职特性全文并把小节标签从「子职特性追加」改为「子职特性增强」；同时补齐 67 处关键词注释（近卫 7 支的「孤立」、狙击 5 支的「不视为通过回想加入配置中」、重装〖守护者〗与辅助〖咒愈师〗与特种〖伏击客〗各 2 处），并按文档重排 244 个字段的染色范围。重刷后与当前文档逐字段比对为 4200/4200 完全一致，14 支仅存在于包内的新子职数据未改动（仅剥离上述重复注释）。

系统包新增「选择其他资源」入口：`modules.json` 增加 `pick-other-resources`（按钮「选择其他资源」，资源库「其他」，可建卡到能力卡桌面），`character-card-table` 的资源来源补 `otherResourceLibraries`；默认 shell 与两个皮肤把领域卡与其他资源选择器并排放入 `.card-picker-grid`，`layouts/shell.css` 提供两列栅格、窄屏（≤1080px）单列堆叠与打印整体隐藏。没有原生入口的自由模板资源（如本说明卡）由路由统一落到「其他资源」库。
