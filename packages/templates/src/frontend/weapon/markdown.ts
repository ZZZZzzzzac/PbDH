export const weaponMarkdownStyles = `
.weapon-description [data-restricted-markdown]{min-height:0;overflow:hidden;color:var(--ink);font:500 2.1px/1.3 "Noto Sans SC",sans-serif}
.weapon-description [data-restricted-markdown] :is(p,ul,ol){overflow:hidden}
`;
