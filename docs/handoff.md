# 开发交接

日期：2026-09-07

## 当前状态

模板升级功能与本轮验收修复已完成，准备发布 `v0.1.6`。发布与部署结果以 GitHub Release / Deploy Release 工作流为准。

## 变更

- 新增敌人模板 `1.0.4`、环境模板 `1.0.3`，特性展开/折叠符号移至名称前，消除冒号前的多余间距；保留旧版本行为。
- 导入、恢复、市场下载保持精确模板版本；metadata 显示“新!”提示，由用户通过共享升级窗口选择目标版本。
- Creator 显式升级后更新资源包版本并标记受影响资源已修改；Market 在原详情页替换归档，保持 Publication ID 和发布状态，不创建工坊副本。
- 发布、工坊编辑、市场编辑共用许可自由输入和独立预设菜单。
- 修复多版本 Schema ID 重复注册：转换校验器按精确模板版本隔离 AJV 注册空间并缓存，不修改历史 Schema。
- 修复市场窄屏作者管理入口隐藏，以及升级数量前多出的 `$`。
- AGENTS.md 记录凭据从 `.env.local` 读取，不记录凭据值。

## 验证证据

- `npm run verify` 通过：106 个 TypeScript 测试文件、867 个测试；Python 151 个测试；类型检查、依赖边界、Contract 检查、渲染测量与构建通过。仅既有 warning。
- 新增 Schema 重复注册回归测试，已确认修复前失败、修复后通过，并确认无效数据仍被拒绝。
- 补充 Creator 升级后正式导出字节的回读断言：模板、资源数据、版本、摘要和媒体一致；相关 18 个测试通过。
- 浏览器确认旧敌人/环境导入保持原版本；敌人 `1.0.0 → 1.0.2 → 1.0.3 → 1.0.4`、环境 `1.0.0 → 1.0.2 → 1.0.3` 升级成功，取消不改变版本。
- 已核对敌人和环境长名称、英文原文、环境空标题回退、连续排版，以及市场环境窄屏放大滚动。
- 三个许可入口的自由输入、菜单、Tab/Enter 操作通过；测试编辑取消。
- 真实登录市场升级成功：`JF的DIY合集` 的 Publication `01a0754e-de65-70d6-8801-64376f97a679` 从包版本 `1.0.0` 更新至 `1.0.1`，74 项资源、已发布状态和 URL 保持不变；刷新后升级结果保持，未创建工坊副本（该次验收目标为敌人 1.0.3、环境 1.0.2、自由 1.0.2）。
- 工坊导出显示成功通知；文件内容回读通过正式导出工作流测试验证。

## 发布流程

按 `deploy/README.md` 推送版本标签，由 Release 工作流完整验证并构建不可变制品，再运行 Deploy Release 更新 `/pbdh_tools/`，最后检查公开版本标记、Backend 健康和页面导航。不切换旧站 `/pbdh/`。
