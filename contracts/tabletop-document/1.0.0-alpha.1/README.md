# Tabletop Document 1.0.0-alpha.1

开发中 Contract。用于 GM 独立桌面文档及 Player Character Data 内嵌桌面模型的共同数据边界；正式发布前不承诺兼容 Reader 或迁移。

逻辑文档只保存实例私有资源副本、运行状态、几何信息和 Asset ID。选择框、当前工具、视口、打开标签、编辑页及 undo/redo 不属于 Contract。

`.pbtab` 是本 Contract 的单桌面便携归档 Profile，包含 `tabletop.json` 与文档引用的规范化媒体。
